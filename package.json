{
  "name": "laserdx",
  "version": "4.42.0",
  "private": true,
  "description": "LaserDx — The Laser Reference, packaged for iOS with Capacitor",
  "scripts": {
    "sync": "npx cap sync ios",
    "copy": "npx cap copy ios",
    "open:ios": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^8.0.0",
    "@capacitor/ios": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0",
    "@capacitor/assets": "^3.0.0"
  }
}
